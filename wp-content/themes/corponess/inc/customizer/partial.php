<?php
/**
* Partial functions
*
* @package Theme Palace
* @subpackage corponess
* @since corponess 1.0.0
*/


// Service Section
if ( ! function_exists( 'corponess_service_title_partial' ) ) :
    // popular destination title
    function corponess_service_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['service_title'] );
    }
endif;

if ( ! function_exists( 'corponess_service_sub_title_partial' ) ) :
    // popular destination title
    function corponess_service_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['service_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_about_title_partial' ) ) :
    // popular destination title
    function corponess_about_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['about_title'] );
    }
endif;

if ( ! function_exists( 'corponess_about_description_partial' ) ) :
    // popular destination title
    function corponess_about_description_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['about_description'] );
    }
endif;

if ( ! function_exists( 'corponess_about_btn_title_partial' ) ) :
    // popular destination title
    function corponess_about_btn_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['about_btn_title'] );
    }
endif;

if ( ! function_exists( 'corponess_client_title_partial' ) ) :
    // popular destination title
    function corponess_client_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['client_title'] );
    }
endif;

if ( ! function_exists( 'corponess_client_sub_title_partial' ) ) :
    // popular destination title
    function corponess_client_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['client_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_project_title_partial' ) ) :
    // popular destination title
    function corponess_project_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['project_title'] );
    }
endif;

if ( ! function_exists( 'corponess_project_sub_title_partial' ) ) :
    // popular destination title
    function corponess_project_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['project_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_project_btn_title_partial' ) ) :
    // popular destination title
    function corponess_project_btn_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['project_btn_title'] );
    }
endif;

if ( ! function_exists( 'corponess_testimonial_sub_title_partial' ) ) :
    // popular destination title
    function corponess_testimonial_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['testimonial_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_testimonial_title_partial' ) ) :
    // popular destination title
    function corponess_testimonial_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['testimonial_title'] );
    }
endif;

if ( ! function_exists( 'corponess_blog_title_partial' ) ) :
    // popular destination title
    function corponess_blog_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['blog_title'] );
    }
endif;

if ( ! function_exists( 'corponess_blog_sub_title_partial' ) ) :
    // popular destination title
    function corponess_blog_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['blog_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_blog_btn_label_partial' ) ) :
    // popular destination title
    function corponess_blog_btn_label_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['blog_btn_label'] );
    }
endif;

if ( ! function_exists( 'corponess_subscribe_title_partial' ) ) :
    // popular destination title
    function corponess_subscribe_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['subscribe_title'] );
    }
endif;

if ( ! function_exists( 'corponess_subscribe_sub_title_partial' ) ) :
    // popular destination title
    function corponess_subscribe_sub_title_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['subscribe_sub_title'] );
    }
endif;

if ( ! function_exists( 'corponess_subscribe_btn_label_partial' ) ) :
    // popular destination title
    function corponess_subscribe_btn_label_partial() {
        $options = corponess_get_theme_options();
        return esc_html( $options['subscribe_btn_label'] );
    }
endif;











